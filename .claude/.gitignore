# macOS
.DS_Store

# Ignore all skills by default, so we can seletively git add the ones we cache
# about sharing and publishing for reuse.
skills/

backups/
# Ephemeral session data and conversation transcripts
cache/
debug/
file-history/
history.jsonl
mcp-needs-auth-cache.json
paste-cache/
plugins/
projects/
session-env/
sessions/
shell-snapshots/
statsig/
tasks/
todos/
transcripts/

# Local-only settings (machine-specific paths, personal experiments)
settings.local.json
