" make searches case-sensitive only if they contain upper-case characters
set ignorecase
set smartcase

" quicker jumping to file counterparts (.h/.m toggling)
map ,c :ncounterpart<cr>

map ,w :w<cr>
